repos:
  - repo: local
    hooks:
      - id: mkdocs-strict
        name: mkdocs build --strict
        entry: mkdocs build --strict --quiet
        language: system
        pass_filenames: false
        files: ^(docs/|mkdocs\.yml$)
